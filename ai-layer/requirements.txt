# Quorum AI layer — runs on Python 3.11+ stdlib only, NO heavy ML deps required.
# Every advisor (technical, trend_ml, sentiment, news) is pure-Python. The `sentiment`
# advisor uses a curated negation/intensity-aware lexicon — no transformers/torch needed,
# so a small server runs the full council reliably alongside the local LLM judge.

# --- pre-built technical indicators (optional quality upgrade) ---
# pandas>=2.0
# pandas-ta>=0.3.14b

# --- optional ML extras (for custom training or additional models) ---
# scikit-learn>=1.4
# numpy>=1.26

# --- testing ---
# pytest>=8.0

# Note: cli.py / aggregator / pipeline run with Python 3.11+ stdlib only
# (every agent has a fallback) — install the optional packages above to upgrade analysis quality

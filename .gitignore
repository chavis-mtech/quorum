# Node / frontend
node_modules/
frontend/dist/
*.log

# Rust / backend — all cargo build dirs (incl. cross-compile output)
backend/target/
target/
target-linux/
**/target-linux/
**/target/
# vendored crate registry (174M) — restored via `cargo fetch`, never committed
.cargo-registry/
**/.cargo-registry/

# Build/deploy artifacts — regenerated by deploy.sh, never committed
/quorum-linux-runtime/
quorum-linux-runtime*.zip
# stray compiled binaries that land in backend/ (build output, not source)
backend/quorum
backend/quorum-linux-amd64
backend/*-amd64

# IDE / editor
.idea/
.vscode/
.claude/settings.local.json

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Secrets / env — ห้ามขึ้น git เด็ดขาด
.env
*.key
secrets.*

# OS
.DS_Store
